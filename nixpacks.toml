[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[variables]
PLAYWRIGHT_BROWSERS_PATH = "/app/.cache/ms-playwright"

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "playwright install chromium"
]

[phases.start]
cmds = ["python3.11 bot.py"]
