[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "python311Packages.playwright"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "playwright install chromium"
]

[phases.start]
cmds = ["python bot.py"]